stages:
  - linting

linting:
  stage: linting
  image: continuumio/miniconda3:latest
  script:
    - pip install git+https://github.com/psf/black
    - pip install isort
    - pip install flake8
    - black --check src buzzard.py
    - isort src buzzard.py --check-only
    - flake8 src buzzard.py
